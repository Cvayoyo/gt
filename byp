data_id = [[
isi email password sama kaya gmail aja
]]

for str in string.gmatch(data_id, "([^\r\n]+)") do
    local email, password= str:gsub(" ",""):match("([^|]+):(.+)")
    addUbiBot(email, password, "",true)
end
